<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bustamante.unifit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_redondeado"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_redondeado"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddRoutinesActivity" />
        <activity android:name=".AddRoutineActivity" />
        <activity android:name=".MyRoutinesActivity" />
        <activity android:name=".EditRoutineActivity" />
        <activity android:name=".SelectAreaActivity"></activity>
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile"></activity>

    </application>

</manifest>